apiVersion: v1
kind: Service
metadata:
  name: magento-db
  namespace: magento2
spec:
  selector:
    app: magento-db
  type: NodePort
  ports:
    - port: 3306
      targetPort: 3306
      nodePort: 30306   # optional, Kubernetes will assign if omitted
